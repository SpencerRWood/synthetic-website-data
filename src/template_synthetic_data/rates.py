"""Rate and proportion placeholders."""

from collections.abc import Iterable
from typing import Any


def apply_rate(
    _records: Iterable[dict[str, Any]],
    _rate: float,
) -> list[dict[str, Any]]:
    """Apply a rate to generated records."""
    # TODO: Apply the requested rate to generated records.
    raise NotImplementedError
